Атрибут in в Swagger используется для определения того, как параметр передаётся в запросе к API. 
Он указывает, где именно будет размещён параметр: 
в URL (path), 
в строке запроса (query), 
в теле запроса (body) или 
в заголовках (headers).